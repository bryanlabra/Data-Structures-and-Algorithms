from setuptools import setup, find_packages

setup(
    name="DataStructuresAndAlgorithms",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        # List your dependencies here, e.g., "numpy>=1.19.2"
    ],
    include_package_data=True,
)